from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a simple deployment of the segment anything model by using fastapi library in python',
    author='scout',
    license='MIT',
)
